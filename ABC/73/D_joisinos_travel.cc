#include<bits/stdc++.h>
constexpr long long INFL = 1LL << 60;
constexpr int INF = 1e9;

using namespace std;

using ll = long long;
using P = pair<int, int>;

int main()
{
    int N, M, R;
    cin >> N >> M >> R;
    int r[8];
    for(int i = 0; i < min(8,N); ++i) cin >> r[i];
    int a[20000],b[20000],c[20000];
    for(int i = 0; i < M; ++i) cin >> a[i] >> b[i] >> c[i];


    return 0;
}

