#include<bits/stdc++.h>
constexpr long long INFL = 1LL << 60;
constexpr int INF = 1 << 30;

using namespace std;

using ll = long long;
using P = tuple<int, int>;
using iarr = valarray<int>;

int main()
{
    vector<string> v(3);
    cin >> v[0] >> v[1] >> v[2];

    cout << 'A' << v[1][0] << 'C' << endl;
    return 0;
}

